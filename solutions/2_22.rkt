;This is because we end up with a nested pair where the first element of the outermost pair is nil.
;Whereas, a sequence is defined to be nested pair where the last element of the innermost pair is nil.
